import { Redirect } from 'expo-router';

export default function Index() {
  return <Redirect href="/landing" />;
}
// This file serves as the entry point for the app, redirecting to the landing page.